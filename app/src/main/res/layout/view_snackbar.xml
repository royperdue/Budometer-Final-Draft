<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/pad80dp"
    tools:parentTag="RelativeLayout">

    <TextView
        android:id="@+id/snackBar_text_bottom_caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@+id/snackBar_button_action"
        android:maxLines="2"
        android:textColor="@color/white"
        tools:text="There is something about you that make me very happy and anxious at the same time"/>

    <Button
        android:id="@+id/snackBar_button_action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="?selectableItemBackground"
        android:textColor="@color/white"
        android:textStyle="bold"
        tools:text="@string/ok"/>
</merge>
